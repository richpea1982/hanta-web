http:
  routers:
    traefik-dashboard:
      rule: "Host(`proxy.{{ traefik_domain }}`)"
      service: "api@internal"
      entryPoints:
        - websecure

  middlewares:
    redirect-to-https:
      redirectScheme:
        scheme: https
