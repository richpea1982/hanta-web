http:
  routers:
    traefik-dashboard:
      rule: "Host(`proxy.{{ traefik_domain }}`)"
      service: "api@internal"
      entryPoints:
        - websecure

    portainer:
      rule: "Host(`portainer.{{ traefik_domain }}`)"
      entryPoints:
        - websecure
      service: portainer
