entryPoints:
  web:
    address: ":80"
  websecure:
    address: ":443"

providers:
  docker:
    exposedByDefault: false
  file:
    filename: /opt/traefik/dynamic.yml
    watch: true

api:
  dashboard: true
  insecure: false   # 👈 ensures dashboard is only reachable via defined router

certificatesResolvers:
  cloudflare:
    acme:
      email: rpearsall1982@gmail.com
      storage: /etc/traefik/acme.json
      dnsChallenge:
        provider: cloudflare
        resolvers:
          - "1.1.1.1:53"
          - "8.8.8.8:53"
