[Unit]
Description=cloudflared tunnel (token)
After=network.target

[Service]
ExecStart=/usr/bin/cloudflared --no-autoupdate tunnel run --token {{ vault_cloudflare_tunnel_token }}
Restart=always
RestartSec=5s

[Install]
WantedBy=multi-user.target
